class Solution {
public:
    void reverseString(vector<char>& s) {
        long long int i = 0;
        long long int j = s.size() - 1;

        while(i < j){
            char temp = s[i];
            s[i] = s[j];
            s[j] = temp;
            i++;
            j--;
        }
    }
};
