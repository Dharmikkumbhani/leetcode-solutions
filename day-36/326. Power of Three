class Solution {
public:
    bool isPowerOfThree(int n) {
        if(n <= 0) return false;
        long double result = log(n) / log(3);
        if (fabsl(result - roundl(result)) < 1e-12) return true;
        return false;
    }
};
